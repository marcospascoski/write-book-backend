# ============================================================================
# WriteBook API - Configuração de Ambiente
# ============================================================================

# Banco de Dados PostgreSQL
DB_PASSWORD=postgres

# Autenticação JWT (IMPORTANTE: Gere um secret forte para produção!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# Migrations Automáticas
# true = Executa migrations na primeira inicialização
# false = Não executa migrations (padrão após primeira instalação)
APPLY_MIGRATIONS=true

# ============================================================================
# Configurações Opcionais
# ============================================================================

# Descomentar para sobrescrever a connection string padrão
# CONNECTION_STRING=Host=postgres;Database=db-write-book;Username=postgres;Password=${DB_PASSWORD}

# Descomentar para definir ambiente específico
# ASPNETCORE_ENVIRONMENT=Production

# ============================================================================
# NOTAS IMPORTANTES:
# ============================================================================
# 1. Este arquivo NÃO deve ser commitado no Git (.gitignore)
# 2. Em produção, SEMPRE gere um JWT_SECRET forte e único
# 3. Use senhas fortes para o banco de dados
# 4. Após primeira instalação, APPLY_MIGRATIONS deve ser false
# 5. Para gerar JWT Secret seguro:
#    - Linux/Mac: openssl rand -base64 64
#    - PowerShell: [Convert]::ToBase64String((1..64|%{Get-Random -Max 256}))
# ============================================================================
